'use strict';

describe('brf.gym module', function() {

  beforeEach(module('brf.gym'));

  describe('gym controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var gymCtrl = $controller('GymCtrl');
      expect(gymCtrl).toBeDefined();
    }));

  });
});
