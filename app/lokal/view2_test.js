'use strict';

describe('brf.lokal module', function() {

  beforeEach(module('brf.lokal'));

  describe('lokal controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var lokalCtrl = $controller('LokalCtrl');
      expect(LokalCtrl).toBeDefined();
    }));

  });
});
